version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - target/calculations-0.0.1-SNAPSHOT.jar
    - target/calculationIU-0.0.1-SNAPSHOT.jar
    - target/calculationCircuitbreaker-0.0.1-SNAPSHOT.jar